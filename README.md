# Приложение "Контакты" с Redux

## Анализ проблем и улучшений

### Проблемы приложения без менеджера состояния:

1. **Дублирование состояния**
   - Данные контактов и групп хранятся в локальном состоянии каждого компонента
   - При переходе между страницами данные теряются и загружаются заново
   - Нет единого источника истины (Single Source of Truth)

2. **Проблемы с передачей данных (Prop Drilling)**
   - Данные передаются через множество уровней компонентов
   - Усложняется поддержка и рефакторинг кода
   - Компоненты становятся сильно связанными

3. **Отсутствие централизованного управления состоянием**
   - Невозможно отследить изменения состояния
   - Сложно отлаживать приложение
   - Нет возможности использовать DevTools для мониторинга

4. **Проблемы с синхронизацией данных**
   - Изменения в одном компоненте не отражаются в других
   - Например, добавление контакта в избранное на одной странице не обновляет другие страницы

5. **Отсутствие обработки асинхронных операций**
   - Нет единого подхода к загрузке данных с сервера
   - Каждый компонент самостоятельно управляет состоянием загрузки

6. **Сложность масштабирования**
   - При добавлении новых функций код становится все более запутанным
   - Сложно добавлять новые страницы и функциональность

### Почему стоит использовать Redux:

1. **Единый источник истины**
   - Все данные хранятся в одном месте (store)
   - Легко получить доступ к данным из любого компонента

2. **Предсказуемость**
   - Изменения состояния происходят только через actions и reducers
   - Легко отследить, что и когда изменилось

3. **Отладка**
   - Redux DevTools позволяют видеть все изменения состояния
   - Time-travel debugging - возможность "откатить" состояние

4. **Масштабируемость**
   - Легко добавлять новые функции
   - Код становится более структурированным и понятным

5. **Переиспользование**
   - Логика работы с данными вынесена из компонентов
   - Компоненты становятся более простыми и переиспользуемыми

6. **Асинхронные операции**
   - Redux Thunk позволяет обрабатывать асинхронные действия
   - Единый подход к загрузке данных с сервера

## Структура проекта

```
redux/
├── src/
│   ├── store/
│   │   ├── index.js                    # Настройка Redux store
│   │   ├── actions/
│   │   │   ├── contactsActions.js     # Actions для контактов
│   │   │   ├── groupsActions.js        # Actions для групп
│   │   │   └── favoritesActions.js     # Actions для избранного
│   │   └── reducers/
│   │       ├── contactsReducer.js     # Reducer для контактов
│   │       ├── groupsReducer.js        # Reducer для групп
│   │       └── favoritesReducer.js     # Reducer для избранного
│   ├── components/
│   │   ├── Layout.jsx                  # Компонент навигации
│   │   └── Layout.css
│   ├── pages/
│   │   ├── ContactsPage.jsx            # Страница всех контактов
│   │   ├── ContactPage.jsx             # Страница контакта
│   │   ├── GroupsPage.jsx              # Страница всех групп
│   │   ├── GroupPage.jsx               # Страница группы
│   │   └── FavoritesPage.jsx           # Страница избранного
│   ├── App.jsx                         # Главный компонент с роутингом
│   ├── App.css
│   └── main.jsx                        # Точка входа
├── package.json
├── vite.config.js
└── index.html
```

## Использованные технологии

- **React** - библиотека для создания UI
- **Redux** - менеджер состояния
- **React Redux** - связка React и Redux
- **Redux Thunk** - middleware для асинхронных действий
- **React Router** - маршрутизация
- **Vite** - сборщик проекта

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build
```

## Основные функции

### Redux Store

Store содержит три основных раздела:
- **contacts** - список контактов, состояние загрузки, фильтры
- **groups** - список групп, состояние загрузки
- **favorites** - список ID избранных контактов

### Actions

#### Контакты
- `fetchContacts()` - загрузка контактов (Thunk)
- `setContactsFilter()` - установка фильтра поиска

#### Группы
- `fetchGroups()` - загрузка групп (Thunk)

#### Избранное
- `addToFavorites()` - добавление в избранное
- `removeFromFavorites()` - удаление из избранного

### Reducers

Все reducers следуют принципам Redux:
- Чистые функции
- Не изменяют исходное состояние
- Возвращают новое состояние

## Особенности реализации

1. **Имитация загрузки данных**
   - Используется `setTimeout` для имитации сетевой задержки
   - Показываются состояния загрузки и ошибок

2. **Фильтрация контактов**
   - Фильтр по имени (поиск)
   - Фильтр по группе
   - Фильтры хранятся в Redux store

3. **Избранное**
   - Состояние избранного синхронизировано между всеми страницами
   - Добавление/удаление из избранного работает глобально

4. **Роутинг**
   - Используется React Router для навигации
   - Все страницы доступны через навигационное меню

## Преимущества текущей реализации

✅ Единый источник данных  
✅ Предсказуемые изменения состояния  
✅ Легкая отладка с Redux DevTools  
✅ Масштабируемая архитектура  
✅ Разделение ответственности  
✅ Переиспользуемые компоненты  
✅ Централизованная обработка ошибок и загрузки  
